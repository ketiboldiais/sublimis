export const root = "/cs/data_structures_and_algorithms/problems/";

# Common Problems

This chapter covers some solutions to common problems involving data
structures and algorithms. The problems are listed below with a quick
description.

<table>
<thead>
<tr>
<th>Problem</th>
<th>Description</th>
<th>Keywords</th>
<th>Dependencies</th>
</tr>
</thead>
<tbody>
<tr>
<td><Link href={`${root}plus_minus`}>Integer Ratios</Link></td>
<td>Compute the integer ratios of an array.</td>
<td>static arrays</td>
<td>none</td>
</tr>
<tr>
<td><Link href={`${root}constrained_sums`}>Constrained Sums</Link></td>
<td>Compute the minimum and maximum sums given ${n-1}$ of ${n}$ integers.</td>
<td>static arrays</td>
<td>none</td>
</tr>
<tr>
<td><Link href={`${root}time_convert`}>Time Convert</Link></td>
<td>Convert a 12-hour time to a 24-hour time.</td>
<td>strings, math</td>
<td>none</td>
</tr>
<tr>
<td><Link href={`${root}angle_convert`}>Angle Convert</Link></td>
<td>Convert from radians to degrees and vice versa.</td>
<td>math, graphics</td>
<td>none</td>
</tr>
</tbody>
</table>
